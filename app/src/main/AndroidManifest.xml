<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mercury.myapp">

    <application
        android:name="br.com.lucas.receitaapp.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReceitasApp">
        <activity
            android:name="br.com.lucas.receitaapp.ui.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name="br.com.lucas.receitaapp.ui.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.com.lucas.receitaapp.ui.main.MainActivity"
            android:exported="false" />
    </application>

</manifest>